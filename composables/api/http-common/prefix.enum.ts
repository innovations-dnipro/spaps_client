import { EndpointsEnumAuth } from './prefix.enum.auth';
import { EndpointsEnumClient } from './prefix.enum.client';

export const EndpointsEnum = {
  Authorization: EndpointsEnumAuth,
  Client: EndpointsEnumClient,
};
